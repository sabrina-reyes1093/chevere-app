import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // To deploy on GitHub Pages instead of Vercel, uncomment these two lines
  // and run `npm run build` — the static site lands in the `out/` folder:
  // output: "export",
  // basePath: "/chevere",
};

export default nextConfig;
